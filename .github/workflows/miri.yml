name: Check

on: push

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clippy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Clippy Check
        uses: actions-rs/clippy-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --all-targets --all-features -- -D warnings
          name: clippy

  rustfmt:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Rustfmt Check
        uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: -- --check
      
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Run Tests With Miri
        uses: actions-rs/cargo@v1
        with:
          command: miri
          args: test

  leak-sanitizer:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Leak Sanitizer
        run: |
          export TSAN_OPTIONS="suppressions=$GITHUB_WORKSPACE/blacklist.txt"
          cargo test
        env:
          CFLAGS: "-fsanitize=memory"
          CXXFLAGS: "-fsanitize=memory"
          RUSTFLAGS: "-Zsanitizer=memory"
          RUSTDOCFLAGS: "-Zsanitizer=memory"

  thread-sanitizer:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Thread Sanitizer
        run: |
          export TSAN_OPTIONS="suppressions=$GITHUB_WORKSPACE/blacklist.txt"
          cargo test
        env:
          CFLAGS: "-fsanitize=memory"
          CXXFLAGS: "-fsanitize=memory"
          RUSTFLAGS: "-Zsanitizer=memory"
          RUSTDOCFLAGS: "-Zsanitizer=memory"

  address-sanitizer:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1
    
      - name: Address Sanitizer
        run: |
          export TSAN_OPTIONS="suppressions=$GITHUB_WORKSPACE/blacklist.txt"
          cargo test
        env:
          CFLAGS: "-fsanitize=memory"
          CXXFLAGS: "-fsanitize=memory"
          RUSTFLAGS: "-Zsanitizer=memory"
          RUSTDOCFLAGS: "-Zsanitizer=memory"

  memory-sanitizer:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clippy with stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: clippy, rustfmt, miri

      - uses: Swatinem/rust-cache@v1

      - name: Memory Sanitizer
        run: |
          export TSAN_OPTIONS="suppressions=$GITHUB_WORKSPACE/blacklist.txt"
          cargo test
        env:
          CFLAGS: "-fsanitize=memory"
          CXXFLAGS: "-fsanitize=memory"
          RUSTFLAGS: "-Zsanitizer=memory"
          RUSTDOCFLAGS: "-Zsanitizer=memory"
